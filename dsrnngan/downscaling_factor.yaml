downscaling_factor: 6  # increase in image size in each dimension
steps: [3, 2]  # list of integers that multiply to the downscaling factor; the generator will use UpSampling2D layers of these sizes, alternating with residual blocks.
